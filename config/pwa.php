<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Penny App Version
    |--------------------------------------------------------------------------
    |
    | Increment this manually on every deploy so the service worker update flow
    | can detect and stage a new version for user-confirmed activation.
    |
    */
    'app_version' => env('PWA_APP_VERSION', '2026.03.11.1'),
];

